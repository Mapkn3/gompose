{
    "credential": {
        "username": "<username>",
        "token": "<API token from http://<jenkins-url>/user/<username>/configure"
    },
    "composePath": "<path to docker-compose.yml>",
    "trackedJobs": [
        {
            "url": "http://<jenkins-url>/job/<project1>/job/<branch>/"
        },
        {
            "url": "http://<jenkins-url>/job/<project2>/job/<branch>/"
        },
        {
            "url": "http://<jenkins-url>/job/<project3>/job/<branch>/"
        }
    ]
}